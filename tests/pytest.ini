[pytest]
markers =
    unit: pure logic, no external deps
    integration: requires Docker (postgres, redis, kafka, clickhouse)
    e2e: full 16-agent pipeline
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
